[phases.setup]
nixPkgs = ["nodejs_18", "bun"]

[phases.install]
cmds = ["bun install --no-save"]

[phases.build]
cmds = ["bun build client.ts --outfile client.js --target browser"]

[start]
cmd = "bun run start"
